# Handoff — handoff-05/06/26,20:46

**Session focus:** A data-analytics / "is the edge real?" validation pass on the **live paper bots**. User will decide next session's focus after reading this.

---

## TL;DR for the next agent

- The live bots are **Bot K (SM confirmation)** and **Bot K2 (Bot K + directional L1 floor)** — both paper, running A/B. **Everything else, including Bot G, is RETIRED.**
- **Do NOT analyse or mention Bot G.** It was decommissioned 2026-06-01. The user was explicit and is annoyed that it kept coming up — the stale repo `CLAUDE.md` mislabelled it as active and sent the first analysis down the wrong path. Stay on K / K2 only.
- Built a read-only edge-validation tool and confirmed **both live bots show a statistically significant positive paper edge**. The one gate before live capital is **maker-fill realism** (paper assumes no adverse selection).

## What was done

1. Built [backtest/validate_edge.py](../backtest/validate_edge.py) — read-only, ground-truth edge analysis over resolved trades (no outcome reconstruction). Produces calibration curves, an edge test (est vs market-implied), equity curves, and a scorecard with **day-clustered significance** (the honest metric — trade-level t-stats are inflated by within-day correlation).
2. Ran it → outputs in `backtest/edge_validation/` (`<bot>/*.png` + `scorecard.txt`). See `scorecard.txt` for the full numbers; not duplicated here.

## Key findings (live bots)

| | Bot K (SM confirm) | Bot K2 (L1 floor) |
|---|---|---|
| Resolved trades | 2,894 | 807 |
| Day-clustered significance | **t = 3.60**, 26 days, 20/26 positive | **t = 2.94**, 8 days, 6/8 positive |
| Brier model vs market | 0.248 < 0.250 (**beats market**) | 0.247 < 0.256 (**beats market**) |
| Paper ROI / return-over-maxDD | +18% / 9.7× | +20% / 6.9× |

- Both bots' `est_prob_up` **beats the market-implied price on Brier** → the signal engine adds information (this is the strongest, lowest-variance evidence of edge).
- The recent **`net_ev_per_share` logic (Jun 2+)** lifted both to ~**+$1.00/trade** — the newest config is the strongest driver.
- Bot K2's window is only **8 days** — significant but young; needs more data before strong conclusions.

## Caveats / open risks

- **All paper (`is_paper=1`).** Bot K is a **maker** strategy → paper fills assume no adverse selection. This is the #1 thing likely to erode the +18–20% ROI live. User's own prior work flags it: `backtest/maker_execution_backtest.py`, note "95% of missed fills are winners."
- Day-clustering assumes days are independent; a sustained regime could still inflate. 20/26 positive days for Bot K is reassuring.

## Docs changed this session

- `CLAUDE.md` line ~20 — "Currently active bots" corrected from Bot G to Bot K + K2.
- Vault `C:\Users\joshc\OneDrive\Desktop\Vault\Projects\5min BTC Bot\BTC5Min_index.md` — quick-links section "Bot G = current active bot" mislabel fixed. **Still stale (left for user):** Phase 4 narrative + architecture diagram still show Bot G alongside K.
- Memory `project_bot_status_2026_04.md` + `MEMORY.md` index — updated to June fleet status.

## Git / artifact state

- New untracked files: `backtest/validate_edge.py`, `backtest/edge_validation/`. **Nothing committed.** No DBs or bot code touched (analysis was fully read-only).
- `validate_edge.py` still lists Bot G in its `BOTS` array for contrast — **user asked to strip it.** Quick edit pending: remove the `bot_g_signal_aligned` entry so the script/outputs cover only the two live bots.

## Candidate next-session threads (user will choose)

1. **Strip Bot G from `validate_edge.py`** (small, was requested).
2. **Maker-fill realism check** — the real go-live gate for Bot K. Likely builds on `backtest/maker_execution_backtest.py`.
3. **More Bot K2 data** before trusting its 8-day result.
4. Bring the remaining stale vault sections (Phase 4, arch diagram) current.

## Suggested skills

- **`/grill-with-docs`** — before any strategy/config change (money-adjacent; project rule).
- **`/tdd`** — mandatory for any trading-logic change (write the failing test first).
- **`/gstack-cso`** — security audit before Bot K goes paper→live.
- **`/gstack-guard`** — keep on for any work touching live/maker execution paths.
