# Handoff — handoff-11/06/26,11:05

**Session focus:** Heavy session — execution-realism research arc (maker fills →
capture-missed-fills → grilled → deferred), regime monitor shipped, repo README
published, and a **resolution-integrity incident (J28) diagnosed, fixed, repaired
and verified** the same day. Next session: **Bot K2 A/B evaluation (~Jun 12) on
corrected data**, then a new investigation of the user's choosing.

---

## TL;DR for the next agent

- Live bots: **Bot K** (SM confirmation, go-live candidate) and **Bot K2** (K +
  directional L1 floor), both paper via `multi_runner.py`, running and healthy
  on post-J28-fix code.
- **Post-repair corrected headline numbers (the ONLY trustworthy ones):**
  Bot K +$2,032 / ROI +16.4% / day-clustered t=3.76 (25/32 +days), beats market
  on Brier. K2 +$509 / +12.4% / t=2.46. Edge intact on honest data.
- **Do NOT trust any pre-repair analysis that included Jun 10–11** — ~$867 of
  paper PnL was fabricated by wrong resolutions (J28) and has been corrected in
  both bot DBs. The famous "+$306 Jun-10 day" was actually −$10.
- Everything is committed AND pushed (`origin/main` @ `434c792`). Vault is in
  sync (decisions J25–J28, progress Phases 22–24, to-do updated).

## What was done (details live in the vault / commits — don't re-derive)

1. **Committed prior session's work** + stripped retired Bot G from
   `backtest/validate_edge.py` (`5239f83`, `6bdd905`).
2. **Spot-at-open mispricing hypothesis → REJECTED** (`8cb60f1`): at |spot−open|
   ≤ $10 the market price is information, not mispricing; fading it is −EV even
   gross.
3. **Regime Monitor agent shipped, all 3 phases** (`3be8836`):
   `python -m agents.regime_monitor {profile|pnl|drift|report}` — daily L1/vol/
   regime profile, PnL by L1 zone, drift alarm (PSI + expected-edge-at-risk).
   In repo `CLAUDE.md`. 22 regression tests.
4. **Threshold provenance investigated** (3 parallel explorers): core weights +
   regime thresholds are walk-forward optimised; entry edges hand-tuned from
   8k+ trade backtests; risk/streak/execution params are convention. No ML
   anywhere live.
5. **ML/DL → DEFERRED** ([[decisions_BTC#J25]]): edge is a displacement fade,
   bottlenecks are execution + regime drift, SNR too low. If ever revisited:
   GBM combiner first.
6. **Maker-fill realism gate → ANSWERED** ([[decisions_BTC#J26]], `61ac85f`):
   edge survives realistic maker fills with ~⅓ haircut (K → ~19–21% ROI), K2
   weaker (~10%). Adverse selection real (missed win% 0.62–0.92) but absorbed.
7. **Capture-missed-fills study + `/grill-with-docs` → chase DEFERRED**
   ([[decisions_BTC#J27]], `7cd4bb1`, `729ed9a`): live already re-posts on GTC
   timeout and captures ~70% of the prize; true chase increment only +16–28%,
   inside proxy error bars. J7 stands. **Per-round GTC fill telemetry specced
   into the T1 probe** (go-live checklist) — decision lands on real fills.
   CONTEXT.md gained "Execution policy vocabulary".
8. **In-depth README.md published** (`7fd4678`).
9. **J28 incident** ([[decisions_BTC#J28]], `2f89175`, `79119a0`, `434c792`):
   PolyBackTest silently stopped serving `btc_price_start` (lapsed tier, no
   401) + the exchange-avg window-open fallback had been dead since May 21 →
   blank TUI OHLC, L1 silently neutralised, resolution fallback fabricating
   always-UP. Fixed (TDD, suite 570 green): aggregator-seeded window open,
   retried+guarded `detect_resolution` (returns UNKNOWN > fabricates), safety
   nets resolve by trade's own slug, persistent logs
   (`data_runtime/multi_runner.log`). DB repair applied + verified; bot
   restarted on fixed code; seeded open confirmed firing in the log.
   **PolyBackTest renewal is now OPTIONAL** (background refinement only).

## Next session — start here

1. **Bot K2 A/B evaluation (to-do priority 1, due ~Jun 12).** Compare K2 vs K
   on corrected data; YES side first (genuinely incremental; NO overlaps J13).
   Decide: go-live candidate (consider tightening deadband toward 0) vs retire.
   **Consider excluding Jun 10–11 windows** — L1 was neutralised (zero open)
   those days even after resolution repair. Note from maker-fill work: K2 is
   the weaker bot under realistic fills (~10% vs ~19-21% ROI, day-t 1.9 vs
   2.6-3.3) — factor into the verdict.
2. **User wants a new investigation** (topic TBD — ask). Candidate threads from
   the to-do: pre-live punch list (requirements.lock, funder wiring, GTC
   telemetry build), wiring regime_monitor into the scheduler, weekend_flip /
   strong-L1-NO residual re-audits.

## Gotchas / context the next agent needs

- **Paper PnL since Jun 3 uses the real fee model** (0.07·p·(1−p) at entry);
  `tools/repair_resolutions.py` self-validates against it — reuse that pattern
  for any DB-touching tool (dry-run default, heartbeat guard, self-check).
- **`would_enter` in signal_ticks is position-contaminated** (0 after the paper
  entry fills) — use `prob_edge`/`required_edge`/`would_pick_side` to
  reconstruct the gate per-tick.
- **EARLY_EXIT trades** hold the exit tag in the `resolution` column; their PnL
  is exit-priced — exclude from resolution-based analyses.
- Trades from windows with `oracle_price_at_open = 0` (Jun-10 06–07h, Jun-11
  08:00→restart) traded **without L1** — exclude from L1-conditioned analyses.
- The runner now logs to `data_runtime/multi_runner.log` (rotating) — check it
  first when diagnosing live behaviour.
- Untracked scratch (intentionally left): `Resources.txt`,
  `backtest/analyze_dual_045*.py`, `backtest/conditional_fade_phase12.py`,
  7 pre-June handoffs.

## Suggested skills

- **`/grill-with-docs`** — before acting on the K2 eval verdict (retire vs
  go-live path is money-adjacent) or starting any new strategy thread.
- **`/tdd`** — mandatory for any trading-logic change (e.g. deadband tighten,
  GTC telemetry build).
- **`/diagnose`** — if anything live looks off; the J28 post-mortem pattern
  (trace data path → replay API verbatim → cross-check vs external truth) is
  the house style now.
- **`/gstack-cso`** — re-run before T1 live probe (dependency lockfile still
  the open hard gate).
